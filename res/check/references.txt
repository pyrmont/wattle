# Identifiers named in `src/` and `test/` comments that the tree does not declare.
# Generated by `./res/check/references.janet`; do not edit.
#
# See that file's header for what counts as resolved, which shapes are
# gated, and why the other three are not.
#
# **Class `unresolved` must be empty.** Class `c-era` is the bounded
# backlog Phase 17 Part 2 is working through; it reaches zero when the
# prose pass does.
#
#   c-era      0      (src/ 0, test/ 0)
#   unresolved 0      (src/ 0, test/ 0)
#
#   8366 identifiers cited in comments, 7271 of them in an ungated shape
#
# columns: class  site  name

