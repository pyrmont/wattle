; An example file that errors out. Run it with `wattle examples/error.wattle`
; to see the stack trace of a runtime error.

(defn bork [x]

  (defn bark [y]
    (print "Woof!")
    (print y)
    (error y)
    (print "Woof!"))

  (bark (* 2 x))
  (bark (* 3 x)))

(defn pupper []
  (bork 3)
  1)

(do (pupper) 1)
