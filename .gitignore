# Zig
.zig-cache
zig-out

# The cross-build prefix the Linux recipe in test/README.md installs into
/xbuild

# Bootstrap image (emitted by `zig build image` into the install prefix)
wattle-image.bin

# Objects, libraries and executables built outside zig-out
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.pdb
*.dSYM/

# Editors and operating systems
.vscode
*.swp
.DS_Store

# Local working directories
/tmp
/notes
